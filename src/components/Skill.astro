---
interface Props{
    lightColor?: string,
    darkColor?: string,
    fontColorDark?:string,
}

const { lightColor, darkColor='', fontColorDark = ''} = Astro.props
---

<span class="skill">
    <slot/>
</span>

<style lang="scss" define:vars={{ lightColor, darkColor, fontColorDark }}>
    .skill{
        font-size: .7rem;
        border-radius: 5px;
        padding: .2rem .5rem;
        color: white;
        max-height: 22px;
        font-weight: 600;
        background: var(--lightColor, rgb(71, 71, 71));
    }

    :global(.dark){
        .skill{
            background: #1F6FEB;
            color: var(--fontColorDark, white);
            background: var(--darkColor, #1F6FEB);
        }

        .skill:hover{
            cursor: default;
            color: aliceblue;
            background: rgb(58, 58, 58);
        }
    } 

    .skill:hover{
        background: rgb(117, 117, 117);
    }

    @media print{
        .skill{
            display: none !important;
        }
    }
</style>